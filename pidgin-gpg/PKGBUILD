# Maintainer: Hugo Osvaldo Barrera <hugo@osvaldobarrera.com.ar>

pkgname=pidgin-gpg
pkgver=0.9.1
pkgrel=1
pkgdesc="Transparently encrypts your instant messages with RSA encryption. Easy-to-use, but very secure."
arch=(i686 x86_64)
url="https://github.com/hobarrera/pidgin-gpg"
license=('unknown')
depends=(pidgin)
source=(https://github.com/hobarrera/$pkgname/archive/$pkgver.tar.gz)
md5sums=('98e4ac9f8dcc323fcd84ba8dc8182ff9')

build() {
  cd "$srcdir/$pkgname-$pkgver"

  autoreconf -i
  ./configure
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"
  install -Dm 644 src/.libs/pidgin_gpg.so "$pkgdir/usr/lib/pidgin/pidgin_gpg.so"
}
